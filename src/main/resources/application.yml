server:
 port: 8888

cors:
 originPatterns: http://localhost:4200,http://localhost:8888
  
atom:
  app:
   jwtExpirationMs: 60000
   jwtRefreshExpirationMs: 120000
   jwtSecret: 1658
   jwtCookieName: bezkoder-jwt
   jwtRefreshCookieName: bezkoder-jwt-refresh
 
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring-security-jwt_db?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: admin
    password: 1234

  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    open-in-view: false